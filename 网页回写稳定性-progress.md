# 网页回写稳定性：2026-09-13

## 首次快捷键失败的时序修复

新增实机日志出现四组首次 `content_changed`、再次成功；用户报告引用及 Codex 输入框同样如此。这些输入经过共同的 browserPaste 路径。

代码发现 AX 设置选区后立即读回，未等待异步应用；第一次取消可能留下随后生效的选区，使第二次成功。已针对该模式建立生产选区准备函数的回归：模拟先返回旧选区、两次等待后确认新选区；旧立即检查行为实际红测失败。修复在同一次请求内只设置一次选区，最多等待20次、每次10ms；每轮核对目标与原文，只允许旧选区等待或期望选区就绪，其他变化立即取消。只在就绪后准备剪贴板及粘贴，既不重译也不重发粘贴。

增加就绪零等待、永久未就绪超时、目标/内容变化、取消的测试，共231项检查通过（Core98/App73/Regression60）。实际 AX 调用本身也可能耗时，因此200ms是轮询睡眠预算，不是整个流程硬超时。新增确认轮询日志与 `selection_update_timed_out` 分类。

这是已复现的代码时序缺陷修复，尚需新版真实 X/Codex 的一次物理快捷键验证，不能提前认定全部首次失败已解决。

## 已有实机证据

- 用户确认 X 首页翻译回写成功，之后报告回复框失败。
- 11:55:15、11:56:08、11:56:26 的应用日志均显示捕获成功后回写失败；11:55:52 回写成功。旧日志隐藏错误详情，也没有记录回写模式，不能确定失败分支。
- Computer Use 的定向快捷键没有产生 KEYI 全局快捷键事件，不能当作翻译测试；用户物理按键能触发。当前缺少可无人值守的真实 X 复现闭环。
- 本轮将所选引擎由 DeepSeek 切到 appleSystem，保留其他偏好与 API 配置。苹果引擎仍需实机触发确认；配置切换不等于翻译通过。

## 代码确认的边界

1. 网页输入不能降级为直接写 AXValue。它可能仅改变显示文字而未更新受控编辑器状态。
2. browserPaste 已检查目标、原文、选区与剪贴板归属，并以原生粘贴写入；不能把这些已有检查误称为待实现方案。
3. browserPaste 在 800ms 内看到一次完整匹配即返回；这不能证明稍后不回滚，也不能证明网页内部提交状态正确。
4. keyboardPaste 的旧路径只等待 120ms，没有完整读回验证；旧成功日志不能证明所有网页路径成功。
5. AX 身份连续性依赖窗口、父节点、identifier 等；不能只凭文字相同把另一个编辑器认作原目标。

以上不代表已确认此次 X 失败根因。没有复现证据前，不放宽目标检查、不自动重复粘贴，也不以增加固定等待作为修复。

## 验收矩阵

本轮实现与验证：禁止网页路径降级直写 AXValue；新增无文本 diagnosticCode 与 provider/writeMode 日志。回归用旧逻辑先失败，修复后 Core 98、App 73、Regression 55，共 226 项通过。Release 测试包已构建并通过签名验证；运行中 PID 47801 的启动日志确认 `Translation provider loaded: appleSystem`，快捷键为 `⌃⌥⌘T`。尚未取得苹果翻译的真实 X 完成记录。

| 场景 | 必须观察的结果 | 当前实机状态 |
| --- | --- | --- |
| 首页全文 | 译文完整写回，原文被正确替换 | 用户确认过成功；待苹果引擎复验 |
| 回复弹窗、详情页回复 | 与首页相同，焦点不跳走 | 已报告回复失败，待明确具体分支 |
| 局部选区 | 前后文字逐字保留 | 未测 |
| 多行、空行、emoji、中英混排 | 不丢行、不截断、不破坏 UTF-16 选区 | 未测 |
| 翻译时修改原文、切换输入框/tab | 取消回写，保留用户新内容 | 未测 |
| 编辑器重建 | 只能在证实同一目标时继续 | 未测 |
| 输入后再次编辑/失焦 | 译文不回滚 | 未测 |
| 重复触发、粘贴延迟 | 不重复插入，不报假成功 | 未测 |
| 空输入 | 不改内容、不发送请求 | 未测 |

## 完成标准与后续路线

先以苹果翻译复现并记录 provider、write mode、失败类别，再针对实际分支修复。真实 X 测试只使用草稿，不发帖或发送回复。

本地 ControlledEditor.html 可以验证 DOM 显示与 input 驱动的内部状态是否一致，适合增加延迟、回滚、多编辑器和重建变体，但它不是 X 的实现，不能替代 X 验收。

通用 AX 方案无法证明任意网站内部状态。若要更高保证，需要针对支持的网站增加浏览器侧适配与测试；浏览器扩展属于额外架构工作，本轮未实现或安装。无可靠确认时应提供明确失败/未确认状态和人工复制退路，不能保证所有网站及未来改版永不失败。
