# KEYI 可译使用说明

KEYI 可译会把当前输入框里的文字翻译成你选择的语言，并直接替换原文。它不会自动发送消息。

## 快速开始

1. 启动 KEYI 可译。
2. 打开 macOS 菜单栏或 Windows 系统托盘中的 KEYI。
3. 选择“翻译服务”和“目标语言”。
4. 点回要输入文字的地方：选中文字时只翻译选中部分；不选时翻译输入框全文。
5. 按快捷键。macOS 默认是 `⌥T`，Windows 默认是 `Alt+T`，实际按键以 KEYI 设置显示为准。

翻译时先不要切换窗口或继续打字。

## macOS 第一次使用

1. 下载 DMG 后，将“KEYI 可译”拖到 `Applications`，再从“应用程序”打开。
2. 本实验版未经过 Apple 公证。若 macOS 拦截应用，在 Finder 中按住 Control 点按“KEYI 可译”，选择“打开”；仍被拦截时，到“系统设置 → 隐私与安全性”底部选择“仍要打开”。不要关闭 Gatekeeper。
3. 打开 KEYI 后，在设置的“通用”页授予辅助功能权限，或在“系统设置 → 隐私与安全性 → 辅助功能”中打开 KEYI 可译。授权后退出并重新打开 KEYI。
4. 回到 KEYI。只想直接使用时，选择“系统翻译”，不用填写 API Key。

第一次使用某个语言组合时，系统会准备对应的翻译语言资源。保持联网，按 macOS 提示下载或继续；完成后再次翻译。系统未提供该语言组合、下载失败或长期无响应时，可稍后联网重试，或改用云端翻译服务。系统翻译只使用目标语言，不应用“使用场景”和“表达风格”。

## Windows 第一次使用

1. 从 [KEYI Releases](https://github.com/oisano11/KEYI/releases) 下载对应版本的 Windows 安装包并运行。
2. 启动后，在任务栏右下角找到 KEYI 图标；图标被收起时，先点“显示隐藏的图标”。
3. 右键 KEYI，打开“设置...”，在“翻译服务”中选择服务并填写 API Key。Endpoint 和模型名不确定时保留默认值。
4. 保存后，点击“用于翻译”。

安装时如果 Windows 显示“未知发布者”，请先确认安装包来自上面的官方发布页，再选择“更多信息 → 仍要运行”。

## 翻译设置

- “目标语言”决定翻译成哪种语言。
- “使用场景”决定这句话用在哪，例如聊天、发帖或商务。
- “表达风格”只在目标语言为英语时生效；商务和贴近原文会关闭表达风格，优先保证准确或贴近原文。
- API Key 在本机保存。使用云端服务（DeepSeek、通义千问、火山方舟、Grok、自定义服务）时，本次选中的待翻译文字会发送给你选择的服务；不会上传整个输入框。本地模型只在本机处理。

## 遇到问题

### 按快捷键没有反应

确认 KEYI 正在运行，并在设置中查看当前快捷键。快捷键被其他软件占用时，点击“设置快捷键…”换一个。

### 提示“需要辅助功能权限”（macOS）

打开“系统设置 → 隐私与安全性 → 辅助功能”，允许 KEYI 可译，然后退出并重新打开 KEYI。

### macOS 提示无法验证开发者或无法打开

实验版使用本机签名但未经过 Apple 公证。请在 Finder 中按住 Control 点按“KEYI 可译”，选择“打开”；如仍被拦截，先尝试打开一次，再到“系统设置 → 隐私与安全性”底部选择“仍要打开”。不要关闭 Gatekeeper 或执行来源不明的终端命令。

### 系统翻译一直准备中或没有结果（macOS）

确认已经选择“系统翻译”和目标语言，并保持联网。第一次使用该语言组合时，macOS 必须下载对应的翻译语言资源；按系统提示完成下载后重试。若系统不支持该语言组合，改用云端翻译服务。

### 提示“请先配置翻译服务”

打开“设置... → 翻译服务”，填写并保存对应服务的 API Key，再选择“用于翻译”。macOS 也可以改用不需要 API Key 的“系统翻译”。

### 提示找不到输入框或无法读取文字

先点击目标输入框，再重试。密码框、只读区域以及部分特殊输入框不能翻译。Windows 微信或 WhatsApp 中，建议先明确选中要翻译的文字。

### 翻译完成但没有写回

翻译期间不要切换窗口、移动光标或修改原文。KEYI 发现输入框已经变化时会取消写回，避免覆盖你新输入的内容。

### Windows 某些程序里不能使用

KEYI 和目标程序需要处于相同权限级别。目标程序以管理员身份运行时，普通权限的 KEYI 无法向其中写入文字。

### 翻译结果不符合预期

检查目标语言是否选对；模型服务可以调整“使用场景”和“表达风格”。商务内容建议使用“商务”。

### Windows 如何更新

实验版不提供受支持的自动更新。正式版本发布后，请从 [KEYI Releases](https://github.com/oisano11/KEYI/releases) 手动下载新安装包，并按发布页提供的 SHA-256 校验值核对文件。

## 当前版本

- macOS：`v1.1.6`
- Windows：`v1.1.6`
